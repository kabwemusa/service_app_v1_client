# Copy this file to .env and set the value for your environment.
# Run `ipconfig` (Windows) or `ifconfig` (Mac/Linux) to find your LAN IP.
# For production EAS builds, set this to the deployed API URL.
#EXPO_PUBLIC_API_URL=http://<YOUR_LAN_IP>:8000/api
EXPO_PUBLIC_API_URL=http://localhost:8000/api
